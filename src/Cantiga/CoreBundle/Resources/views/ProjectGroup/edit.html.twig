{% extends 'CantigaCoreBundle:layout:common-layout.html.twig' %}

{% block box_header %}
	<h4>{{ 'Edit group: 0' | trans([item.name]) }}</h4>
{% endblock %}

{% block box_body %}
	{{ form_start(form) }}
	{{ form_row(form.name) }}
	{{ form_row(form.category) }}
	{{ form_row(form.notes) }}
	{{ form_end(form) }}
{% endblock %}