<li class="dropdown help-menu">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
        <i class="fa fa-question-circle"></i>
    </a>
    <ul class="dropdown-menu">
		{% for page in pages %}
        <li><a href="{{ spath(route, {'page': page.route}) }}">{{ page.title | trans([], 'pages') }}</a></li>
		{% endfor %}
    </ul>
</li>